# Delivery food - restaurant-api

```bash
docker compose up -d
```

```bash
docker compose stop
```

```bash
docker compose down
```

```bash
docker container ls -a
```


```bash
mysql --host=127.0.0.1 --port=13306 -u root -p
```